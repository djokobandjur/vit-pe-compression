# v2.0.0 — Neural Networks submission replication release

**Prepared:** 2026-09-06  
**Paper:** *Intervention Geometry Shapes Apparent Positional-Encoding Robustness in Vision Transformers*  
**Authors:** Đoko Banđur and Miloš Banđur

## Why this is a major release

The historical `v1.0.0` release archived the original 876-configuration compression grid and the earlier manuscript framing. That release remains immutable provenance.

The final Neural Networks manuscript is built around a later controlled-intervention program. Version `v2.0.0` is therefore a new authoritative replication release rather than an edit of the historical tag.

## Public release asset

Upload the following file as the release asset for tag `v2.0.0`:

`VITPECOMP_NN_PUBLIC_REPLICATION_v2_0_0_20260906.zip`

SHA-256:

`00ca5b071de16c5c5930ea1901c02e3e0dc86a32a64d9bb776a1b4b3498d5d6e`

The archive contains:

1. `VITPECOMP_NN_SCIENTIFIC_FINAL_v0_20_20260902.zip` — frozen scientific master, complete executed evidence/protocol/provenance layer;
2. `VITPECOMP_NN_MAIN_SUPPLEMENT_v0_28_4_LATEST_20260906.zip` — final Main/Supplement presentation and submission layer;
3. `VITPECOMP_NN_EM_SOURCE_PACKAGE_v0_28_4_20260906.zip` — minimal clean Editorial Manager source trees;
4. `SHA256SUMS.txt`, release README, citation metadata, and license-scope note.

## Scientific-release status

The frozen scientific master records:

- 276/276 numeric verification checks PASS;
- 84/84 Monte-Carlo precision-extension evaluations complete;
- 210/210 scientific-master manifest entries verified;
- 501/501 governance v1.39 manifest entries verified;
- zero missing, extra, or mismatched files after package verification.

No scientific experiment is reopened by the later manuscript/presentation layers.

## Main controlled results represented in v2.0.0

- exact-count/depth-topology control at balanced `K=48`;
- frozen balanced-`K=72` selector replication;
- realized-operator audit across positional-encoding families;
- additive input-scale audit;
- scalar physical/functional displacement diagnostics and within-family reversals;
- post-hoc invariant-preserving ALiBi LOW/MID/HIGH slope-permutation bracket;
- selector-held-out, random-repeat, DTCD, Taylor-omission, and Monte-Carlo sensitivity analyses;
- exact checkpoint and sample/calibration identities with SHA-256 provenance.

## Epistemic-role note

The balanced-`K=72` selector replication is a separately frozen same-estimand replication layer. The ALiBi LOW/MID/HIGH complete-derangement bracket is explicitly **post-hoc** and should not be described as preregistered or as a random sample of the complete slope-assignment space.

## Zenodo

The GitHub repository is already associated with a historical Zenodo record from the `v1.0.0` release. After this branch is merged, create/publish GitHub release `v2.0.0` with the public replication ZIP attached and let the enabled GitHub–Zenodo integration archive the new release. Use the resulting **v2.0.0 version DOI** in the final manuscript. Do not reuse the historical release DOI as the version DOI of the final submission package.

After Zenodo publishes the new version, update:

- the repository README;
- `CITATION.cff`;
- the Main manuscript Data and code availability statement;
- the Supplement provenance statement;
- the final Editorial Manager source package.
